import React from 'react';
import { StyleSheet, View } from 'react-native';
import Colors from '../constants/Colors';

export const WhiteSpace = () => <View style={style.whiteSpace} />

const style = StyleSheet.create({
  whiteSpace:{
    height: 30,
  },
})